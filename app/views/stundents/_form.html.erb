<%= form_with(model: stundent) do |form| %>
  <% if stundent.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(stundent.errors.count, "error") %> prohibited this stundent from being saved:</h2>

      <ul>
        <% stundent.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :birth, style: "display: block" %>
    <%= form.date_field :birth %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
